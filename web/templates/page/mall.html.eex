<!DOCTYPE html>
<html lang="zh-cmn-Hans">
  <head>
    <meta charset="utf-8"/>
    <title> <%= gettext "Firevale Platform Mall Page" %> </title>
    <meta name="viewport" content="width=device-width,initial-scale=1, minimum-scale=1, maximum-scale=1, user-scalable=no"/>
    <meta name="apple-mobile-web-app-capable" content="yes"/>
    <meta name="full-screen" content="true"/>
    <meta name="screen-orientation" content="portrait"/>
    <meta name="x5-fullscreen" content="true"/>
    <meta name="360-fullscreen" content="true"/>
    <link href="<%= static_url(@conn, "/css/mall.css") %>" rel="stylesheet"/>
  </head>
  <body class="<%= @conn.private[:acs_body_class] %>">
    <div id="app">
      <router-view></router-view>
    </div>
    <script>
      window.acsConfig = {
        csrfToken: '<%= get_csrf_token() %>',
        inApp: <%= @conn.private[:acs_browser] == "webview" || @conn.private[:acs_platform] == "wp8" %>,
        platform: '<%= @platform %>',
        locale: '<%= @locale %>',
        appId: '<%= @conn.private[:acs_app_id] %>',
        deviceId: '<%= @conn.private[:acs_device_id] %>',
      }
    </script>
    <script src="<%= static_url(@conn, "/js/mall_commons.js") %>"></script>
    <script src="<%= static_url(@conn, "/js/mall.js") %>"></script>
  </body>
</html>
